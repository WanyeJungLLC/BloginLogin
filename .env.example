# BloginLogin - Environment Variables
# Copy this file to .env and update the values as needed

# ===========================================
# ADMIN CREDENTIALS (Required for first run)
# ===========================================
# These credentials are used to create the initial admin account.
# 
# DEVELOPMENT MODE (NODE_ENV=development):
# - The admin account is created automatically when the app starts
#   if no admin exists in the database.
# - If not set, uses the defaults shown below (John Stamos, etc.)
#
# PRODUCTION MODE (default):
# - Admin will NOT be auto-created unless you set all three variables:
#   ADMIN_USERNAME, ADMIN_PASSWORD, and ADMIN_EMAIL
# - Leave these blank if you prefer to create admin manually in the database
#
# IMPORTANT SECURITY WARNINGS:
# ⚠️  DO NOT use these default values in production!
# ⚠️  Change your password immediately after first login via /admin/settings
# ⚠️  Store production secrets in your platform's secret manager (Replit Secrets, etc.)
# ⚠️  Use a strong ADMIN_PASSWORD: minimum 8 characters, consider using a passphrase
#     Examples: "correct-horse-battery-staple-2026" or use a password manager
#
ADMIN_USERNAME=John Stamos
ADMIN_PASSWORD=1sR3aLLyAcL0n3!
ADMIN_EMAIL=change-me@example.com

# ===========================================
# DATABASE
# ===========================================
# On Replit: DATABASE_URL is automatically set by the platform.
# For local development: Set your PostgreSQL connection string.
#
# ⚠️  PRODUCTION SECURITY REQUIREMENTS:
# - Replace with your own unique database name (not "bloginlogin")
# - Use a strong, random password (32+ characters recommended)
# - Create a dedicated database user with least-privilege access
# - For remote databases, add ?sslmode=require to enable SSL encryption
# - Never expose your PostgreSQL port publicly; keep it internal
#
# Example production DATABASE_URL with SSL:
# DATABASE_URL=postgresql://bloginlogin_app:STRONG_RANDOM_PASSWORD@your-db-host:5432/bloginlogin_prod?sslmode=require
#
# For local development with docker-compose.yml provided:
DATABASE_URL=postgresql://bloginlogin:bloginlogin@localhost:5432/bloginlogin

# ===========================================
# STORAGE PROVIDER
# ===========================================
# On Replit: Omit this variable to use Replit Object Storage automatically.
# For local development: Set to "local" to store files in ./uploads folder.
#
# SECURITY NOTES:
# - Local storage (./uploads) serves all files publicly at /uploads/*
# - Use local storage only for public assets (blog images, portfolio screenshots)
# - For protected files, use Replit Object Storage with ACLs and auth checks
#
STORAGE_PROVIDER=local

# ===========================================
# SESSION SECRET (Optional)
# ===========================================
# Used to sign session cookies. Auto-generated if not set.
# For production, set a strong random string.
#
# SESSION_SECRET=your-secure-random-string-here

# ===========================================
# REPLIT OBJECT STORAGE (Replit only - auto-configured)
# ===========================================
# These are set automatically when using Replit Object Storage.
# Leave commented for local development.
#
# DEFAULT_OBJECT_STORAGE_BUCKET_ID=your-bucket-id
# PUBLIC_OBJECT_SEARCH_PATHS=public
# PRIVATE_OBJECT_DIR=.private
